---
title: middleware 属性
description: 为应用程序的特定页面设置中间件。
navigation.title: Middleware Property
category: components-glossary
---
# middleware 属性

为应用程序的特定页面设置中间件。

---

- 类型: `String` 或 `Array` 或 `Function`
  - 元素: `String` 或 `Function`

## 命名中间件

通过在 `middleware/` 目录中创建文件可以创建命名中间件，文件名即为中间件名称。

```js{}[middleware/authenticated.js]
export default function ({ store, redirect }) {
  // 如果用户未认证
  if (!store.state.authenticated) {
    return redirect('/login')
  }
}
```

```html{}[pages/secret.vue]
<template>
  <h1>Secret page</h1>
</template>

<script>
  export default {
    middleware: 'authenticated'
  }
</script>
```

## 匿名中间件

如果只需要在特定页面使用中间件，可以直接使用函数（或函数数组）：

```html{}[pages/secret.vue]
<template>
  <h1>Secret page</h1>
</template>

<script>
  export default {
    middleware({ store, redirect }) {
      // 如果用户未认证
      if (!store.state.authenticated) {
        return redirect('/login')
      }
    }
  }
</script>
```
